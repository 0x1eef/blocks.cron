#!/bin/sh -e

##
# Require root.
if [ "$(id -u)" != "0" ]; then
    echo "You must be root to run this script."
    exit 1
fi

##
# Dependencies
. setup/platform.sh
. setup/doas.sh
. setup/user.sh
. setup/cron.sh

##
# main
create_user
create_home
install_allowfile
install_crontab
install_doas_conf
