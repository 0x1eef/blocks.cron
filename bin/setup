#!/bin/sh
set -e

##
# Confirm user is root
if [ $(id -u) -ne 0 ]; then
    echo "You must be root to run this script."
    exit 1
fi

##
# Dependencies
. setup/user.sh
. setup/home.sh

##
# main
create_user
create_home
